export enum RNames {
  // common
  slug = 'slug',
  slugParam = ':' + RNames.slug,


  // account
  account = 'account',

  // admin
  admin = 'admin',
  dash = 'dashboard',
  details = 'details',
  edit = 'edit',
  delete = 'delete',

  // movies
  movies = 'movies',
  genres = 'genres',
  collections = 'collections',
  actors = 'actors',

  // auth
  login = 'login',
  register = 'register',
  forgotPassword = 'forgot-password',

  // errors
  error404 = '404',
  videos = 'videos'
}

